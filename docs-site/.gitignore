node_modules
.vitepress/cache
.vitepress/dist

# Everything below is copied out of docs/ and openspec/ by
# scripts/sync-content.mjs at build time — the source of truth stays in the
# repo root, so these must never be committed or hand-edited.
guide/
design/
specs/
public/screenshots/
public/icon.svg
